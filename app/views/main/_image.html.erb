<%= form_for @img, url: {action: "create"}, remote: true do |f| %>
<%= f.hidden_field :code, :value=> @id %>
<%= f.hidden_field :branch, :value=> @parmbranch %>
<%= f.file_field :photo, :required=> true %>
<%= link_to raw('<i class="fa fa-angle-double-left">‌</i>'), '#', id: 'back', remote: true, class: 'btn alert' %>
<%= f.submit "Upload", :id=>'select', class: 'btn alert' %>
<% end %>

